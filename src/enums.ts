export enum ExternalIOType {
  DATABASE = 'DATABASE',
  FILESYSTEM = 'FILESYSTEM',
  NETWORK = 'NETWORK',
  OTHER = 'OTHER',
}

export enum PacketDirection {
  INCOMING = 'INCOMING',
  OUTGOING = 'OUTGOING',
}
